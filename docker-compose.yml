version: '3.8'

services:
  app:
    build: .
    container_name: zhiyan-assistant
    ports:
      - "5000:5000"
    volumes:
      - ./outputs:/app/outputs
      - ./uploads:/app/uploads
      - ./.env:/app/.env:ro
    restart: unless-stopped
    environment:
      - FLASK_ENV=production
